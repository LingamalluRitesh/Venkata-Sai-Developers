# TODO

## Mobile hero heading fix (Pure Water, Healthy Life)
- [ ] Update `src/components/Hero.tsx` to remove hard-coded `top-75 left-110` positioning.
- [ ] Make heading responsive on mobile: center horizontally, use safe top spacing, allow wrapping or scale down.
- [ ] Remove `whitespace-nowrap` on small screens so the text won’t overflow off-screen.
- [x] Build (`npm run build`) and confirm no TS/React issues.
- [ ] (Optional) Re-check mobile layout in browser dev tools.


